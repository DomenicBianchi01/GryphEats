init_db - drops old tables and creates new tables with dummy data
delete_test - drops all tables, creates fresh tables, fills them with dummy data and deletes them (tests the deletion and creation process)
create_tables - creates all empty tables
drop_tables - drops all tables
dummy_data - fills all tables with dummy data for testing purposes (WIP)
delete_dummy - delets all dummy data from tables
