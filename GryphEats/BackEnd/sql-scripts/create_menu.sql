create table menu (menuid int not null, foodid int not null, restaurantid int not null, title varchar(255) not null, description varchar(255), isactive int not null, primary key (menuid, foodid, restaurantid), foreign key (foodid) references food(foodid), foreign key (restaurantid) references restaurant(restaurantid))
